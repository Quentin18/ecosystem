#pragma once

// Window
const int WINDOW_WIDTH = 1000;
const int WINDOW_HEIGHT = 800;
const char WINDOW_TITLE[] = "Ecosystem";
const int FRAMERATE_LIMIT = 60;

// Cells
const float CELL_MIN_RADIUS = 5;
const float CELL_MAX_RADIUS = 10;
